// import { 
//   
// } from '../actions/reporter';

const initialState = {

};

export default function reporter(state = initialState, action) {
  switch (action.type) {
  case "TODO":
    return state;
  default:
    return state;
  }
}
